# Phase 10.6 — filler clips live here.
# filler-1.mp3 … filler-5.mp3 are generated by `npm run generate-fillers`
# (ADMIN, one-time) and committed separately. This keeps the directory in
# git so the /voice fetch path exists before the MP3s are added.
